see "Desktop" folder to derive Oracle and MS Sql Server configuration files (modifying those ones here placed for mysql only) 

the file "start.html" is searched by the framework and must be completed in the section below :
<the domain>[:<the port>]/<the application context>